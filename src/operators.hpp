#pragma once

enum class RelationalOperator
{
	EQU,
	DIFF,
	INF,
	SUP,
	INFE,
	SUPE,
	WTFR
};

enum class AdditiveOperator
{
	ADD,
	SUB,
	OR,
	WTFA
};

enum class MultiplicativeOperator
{
	MUL,
	DIV,
	MOD,
	AND,
	WTFM
};
