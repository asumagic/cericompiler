VAR a : CHAR.

(* yes, really *)

a := 'H'; DISPLAY a;       (* H *)
a := CHAR(101); DISPLAY a; (* e *)
a := 'l'; DISPLAY a;       (* l *)
DISPLAY a;                 (* l *)
a := CHAR(111); DISPLAY a; (* o *)
a := ','; DISPLAY a;       (* , *)
a := CHAR(32); DISPLAY a;  (*   *)
a := 'w'; DISPLAY a;       (* w *)
a := CHAR(111); DISPLAY a; (* o *)
a := 'r'; DISPLAY a;       (* r *)
a := CHAR(108); DISPLAY a; (* l *)
a := 'd'; DISPLAY a;       (* d *)
a := CHAR(33); DISPLAY a.  (* ! *)
