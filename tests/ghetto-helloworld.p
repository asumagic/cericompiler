VAR a : CHAR.

(* yes, really *)

a := CHAR(72); DISPLAY a;  (* H *)
a := CHAR(101); DISPLAY a; (* e *)
a := CHAR(108); DISPLAY a; (* l *)
DISPLAY a;                 (* l *)
a := CHAR(111); DISPLAY a; (* o *)
a := CHAR(44); DISPLAY a;  (* , *)
a := CHAR(32); DISPLAY a;  (*   *)
a := CHAR(119); DISPLAY a; (* w *)
a := CHAR(111); DISPLAY a; (* o *)
a := CHAR(114); DISPLAY a; (* r *)
a := CHAR(108); DISPLAY a; (* l *)
a := CHAR(100); DISPLAY a; (* d *)
a := CHAR(33); DISPLAY a.  (* ! *)
